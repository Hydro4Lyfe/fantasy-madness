export default async function AdminSyncLogsPage() {
  return (
    <main style={{ display: "grid", gap: 12, maxWidth: 900 }}>
      <h1 style={{ margin: 0 }}>Sync Logs</h1>
      <p style={{ margin: 0, opacity: 0.85 }}>TODO: DAL query over SyncLog with filters (feedType, tournamentId, time range).</p>
    </main>
  );
}
