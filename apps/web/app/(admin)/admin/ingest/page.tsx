export default async function AdminIngestPage() {
  return (
    <main style={{ display: "grid", gap: 12, maxWidth: 900 }}>
      <h1 style={{ margin: 0 }}>Ingest Health</h1>
      <p style={{ margin: 0, opacity: 0.85 }}>TODO: show last successful runs, scheduler leases, and error rates.</p>
    </main>
  );
}
